apiVersion: v1
kind: Service
metadata:
  name: {{ include "assessors-studio.fullname" . }}
  labels:
    {{- include "assessors-studio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "assessors-studio.selectorLabels" . | nindent 4 }}
