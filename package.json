{
  "name": "cyclonedx-assessors-studio",
  "version": "0.1.0",
  "private": true,
  "description": "CycloneDX Assessors Studio - Assessment and Attestation Management",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "npm run dev:backend & npm run dev:frontend",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "typecheck": "npm run typecheck:backend && npm run typecheck:frontend",
    "typecheck:backend": "cd backend && npx tsc --noEmit",
    "typecheck:frontend": "npm --prefix frontend run type-check",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "npm --prefix backend run test -- run",
    "test:frontend": "npm --prefix frontend run test -- run",
    "lint": "npm run lint:backend && npm run lint:frontend",
    "lint:backend": "npm --prefix backend exec eslint . || true",
    "lint:frontend": "npm --prefix frontend exec eslint . || true",
    "db:reset": "npm --prefix backend run db:reset",
    "clean": "rm -rf backend/dist backend/data frontend/dist"
  }
}
